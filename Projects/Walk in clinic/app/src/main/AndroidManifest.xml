<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.walk_in_clinic">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AdministatorPage"></activity>
        <activity
            android:name=".AccountCreation"
            android:label="@string/app_name" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RedirectToLogin" />
        <activity android:name=".EmployeePage"/>
        <activity android:name=".PatientPage"/>
        <activity android:name=".ServicePage"/>
        <activity android:name=".ClinicPage"/>
        <activity android:name=".PatientEdit"/>

    </application>

</manifest>